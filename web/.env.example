# CINÉRA Web (Vite) — Environment Template
# Lưu ý: chỉ biến có prefix VITE_ mới được public lên browser.
# API_URL của backend (dev local) — khi deploy đổi thành domain Render.
VITE_API_URL=http://localhost:3000/api